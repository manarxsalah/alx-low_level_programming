#include <stdio.h>
/**
 * main - prints program name
 * @argc: int
 * @argv: list
 * Rrturn: Always 0 (success)
*/
int main(int argc, char const *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
