#include "main.h"
/**
 * add - adds two integers and returns the result.
 *@first: first number sent from main.
 *@second: second number sent from main.
 * Return: result.
*/
int add(int first, int second)
{
	return (first + second);
}
